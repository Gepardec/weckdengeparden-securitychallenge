public class HashGenerator {

    public static String getHash(String str) {
        return hash(str);
    }

    static String hash(String toHash){

        /* FAST GESCHAFFT!
        *
        * Du hast alle Passwörter! Aber vielleicht wäre es besser wenn wir dieses Passwort verschlüsseln.
        * Kannst du eine Methode die unser Passwort in einen Hash verwandelt programmieren?
        *
        *Ob SHA-1,SHA-256 etc. bleibt ganz dir überlassen ;-)
        *
        *
        * */
        return null;
    }
}
